// resources/js/app.js

import 'bootstrap/dist/css/bootstrap.min.css'; // 导入 Bootstrap 的 CSS

// 如果你仍然使用 resources/sass/app.scss 来写你自己的样式：
import '../sass/app.scss'; // 导入你自己的样式
// 或者，如果你决定把自己的样式也改成纯 CSS，可以导入一个css文件，例如 import '../css/app.css';

import './bootstrap'; // 这一行通常包含 Bootstrap JS 和 Axios 等的初始化
